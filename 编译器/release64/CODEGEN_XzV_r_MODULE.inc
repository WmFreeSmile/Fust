'[FILE:C:\Users\WmFreeSmile\Desktop\fust\编译器\modules\宏.Inc
'-----------------------------------------------------------------------------
' 由 VisualFreeBasic 5.8.11 生成的源代码
' 生成时间：2025年03月15日 17时48分58秒
' 更多信息请访问 www.yfvb.com 
'-----------------------------------------------------------------------------



'x    下一级运算
' #macro Left_Value(x) '已经搬到定义文件处
'    Dim 左项 As 项 Ptr, 右项 As 项 Ptr,实参(1) As 类型格式 Ptr '已经搬到定义文件处
'     '已经搬到定义文件处
'    Dim 类型 As 运算符类型 '已经搬到定义文件处
'    x(模式, 编译信息指针, 左项) '已经搬到定义文件处
'    项_返回 = 左项 '已经搬到定义文件处
'     '已经搬到定义文件处
'    If *表达式结果 Is 表达式格式_值=False Then Return '已经搬到定义文件处
'    实参(0) = Cast(表达式格式_值 Ptr, 表达式结果)->值类型 '已经搬到定义文件处
' #endmacro '已经搬到定义文件处

'x    下一级运算
' #macro Right_Value(x) '已经搬到定义文件处
'    x(模式,编译信息指针,右项) '已经搬到定义文件处
'    If *表达式结果 Is 表达式格式_值=False Then 错误信息("非值运算"):表达式结果 = 对象管理.加入(New 表达式格式_空白):Return '已经搬到定义文件处
'    实参(1) = Cast(表达式格式_值 Ptr, 表达式结果)->值类型 '已经搬到定义文件处
'     '已经搬到定义文件处
'    Dim 方法引用 As 节点_方法 Ptr = 重载决议(编译信息指针->运算符方法表(), 对象管理.加入(New 决议线索_运算符(类型)), 实参()) '已经搬到定义文件处
'     '已经搬到定义文件处
'    If 方法引用 = -1 Then 错误信息("找不到合适的方法") : 表达式结果 = 对象管理.加入(New 表达式格式_空白) : Return '已经搬到定义文件处
'    If 方法引用 = 0 Then 错误信息("找到了多个能够接纳实参的方法") : 表达式结果 = 对象管理.加入(New 表达式格式_空白) : Return '已经搬到定义文件处
'  '已经搬到定义文件处
'    If 方法引用->是否函数 Then '已经搬到定义文件处
'       表达式结果=对象管理.加入(New 表达式格式_返回值(方法引用->返回值)) '已经搬到定义文件处
'    Else '已经搬到定义文件处
'       表达式结果=对象管理.加入(New 表达式格式_空白) '已经搬到定义文件处
'    End If '已经搬到定义文件处
'  '已经搬到定义文件处
'    Dim temp As 项_调用 Ptr = 对象管理.加入(New 项_调用) '已经搬到定义文件处
'    temp->方法 = 方法引用 '已经搬到定义文件处
'    ReDim temp->参数(1) '已经搬到定义文件处
'    temp->参数(0) = 左项 '已经搬到定义文件处
'    temp->参数(1) = 右项 '已经搬到定义文件处
'    项_返回 = temp '已经搬到定义文件处
'    左项 = 项_返回 '已经搬到定义文件处
' #endmacro '已经搬到定义文件处
